---
import ServiceCard from './ServiceCard.astro';
---

<section class="services">
	<div class="container">
		<div class="section-header">
			<h2>Core Services</h2>
			<p>Adaptive solutions for complex challenges</p>
		</div>
		<div class="services-grid">
			<ServiceCard 
				title="Auto & Home Systems Repair"
				description="Practical, reliable repairs that last. From automotive diagnostics to home system troubleshooting—we fix it right the first time."
				features={[
					"Transparent pricing guidelines",
					"Emergency repair availability", 
					"Long-term maintenance planning",
					"No-nonsense diagnostics"
				]}
				ctaText="Request Repair Service"
				ctaLink="#contact"
			/>
			<ServiceCard 
				title="Logistics & Adaptive Operations"
				description="Streamlined operations for small businesses and complex projects. We turn chaos into coordinated action."
				features={[
					"Tailored logistics solutions",
					"Crisis response planning",
					"Operational efficiency audits",
					"Scalable system design"
				]}
				ctaText="Schedule Consultation"
				ctaLink="#contact"
			/>
			<ServiceCard 
				title="AI Tools & Digital Infrastructure"
				description="Transparent AI integration that actually serves your needs. No black boxes, no vendor lock-in—just practical digital solutions."
				features={[
					"Ethical AI implementation",
					"Custom automation tools",
					"Digital infrastructure setup",
					"Training and documentation"
				]}
				ctaText="Explore AI Solutions"
				ctaLink="#contact"
			/>
			<ServiceCard 
				title="Emergency & Crisis Response"
				description="When things go wrong, you need someone who can think clearly under pressure. Available for urgent situations that require immediate adaptive response."
				features={[
					"24/7 emergency availability",
					"Rapid assessment protocols",
					"Multi-domain crisis management",
					"Clear communication throughout"
				]}
				ctaText="Emergency Contact"
				ctaLink="mailto:harley@atlasdivisions.com?subject=EMERGENCY%20-%20Atlas%20Divisions"
				isEmergency={true}
			/>
		</div>
	</div>
</section>

<style>
	.services {
		padding: 6rem 0;
		background: var(--color-bg-secondary);
		position: relative;
	}

	.services::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(45deg, transparent 0%, rgba(205, 127, 50, 0.05) 50%, transparent 100%);
		pointer-events: none;
	}

	.container {
		position: relative;
		z-index: 1;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-header h2 {
		font-size: clamp(2rem, 4vw, 3rem);
		color: var(--color-text);
		margin-bottom: 1rem;
		font-weight: 700;
		position: relative;
	}

	.section-header h2::after {
		content: '';
		position: absolute;
		bottom: -1rem;
		left: 50%;
		transform: translateX(-50%);
		width: 60px;
		height: 3px;
		background: linear-gradient(90deg, var(--color-accent-gold), var(--color-accent-bronze));
		border-radius: 2px;
	}

	.section-header p {
		font-size: clamp(1.1rem, 2vw, 1.3rem);
		color: var(--color-text-secondary);
		font-weight: 400;
	}

	.services-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		grid-template-rows: 1fr 1fr;
		gap: 2rem;
		align-items: start;
		max-width: 1200px;
		margin: 0 auto;
	}

	@media (max-width: 992px) {
		.services-grid {
			grid-template-columns: 1fr;
			grid-template-rows: auto;
			gap: 1.5rem;
		}
	}

	@media (max-width: 768px) {
		.services {
			padding: 4rem 0;
		}

		.section-header {
			margin-bottom: 3rem;
		}
	}
</style>